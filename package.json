{
  "name": "cogniq",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "src/frontend",
    "src/backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w src/backend\" \"npm run dev -w src/frontend\"",
    "build": "npm run build -w src/backend && npm run build -w src/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
